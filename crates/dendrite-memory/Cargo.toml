[package]
name = "dendrite-memory"
version = "0.1.0"
edition = "2024"

[dependencies]
dendrite-protocol = { path = "../dendrite-protocol" }
rusqlite = { version = "0.40", features = ["bundled"] }
serde_json = "1"
